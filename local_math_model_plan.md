# 本地数学题识别与讲解方案大纲

## 1. 目标

- 为用户提供可选的完全本地数学题识别与讲解能力。
- 未启用本地模型时，保持现有 N.E.K.O API 路径不变。
- 启用本地模型后，题目、图片和识别结果默认不上传；本地失败时，只有在用户明确同意后才可切换至 API。
- 基础版本不依赖通用视觉大模型，而是采用轻量识别、图文融合和逻辑推理。

## 2. 产品边界与资源红线

| 项目 | 目标 | 发布红线 |
| --- | ---: | ---: |
| 基础本地包（模型与专用运行组件） | 不超过 1 GB | 不超过 1.5 GB |
| 未使用本地能力时的额外内存 | 不超过 300 MB | 不加载模型、不占显存 |
| 单次本地任务峰值内存 | 不超过 3 GB | 不超过 3 GB |
| 单次本地任务峰值显存 | 不超过 3 GB | 不超过 3 GB |
| 模型空闲释放时间 | 60–120 秒 | 不得持续后台占用 |

上述指标须在 Windows 实机 POC 后确认，不能只以模型权重大小作为结论。

## 3. 核心原则

1. **题干优先**：题干明确给出的数学条件直接作为事实，优先级高于图像外观。
2. **图像补充**：图像用于识别题干未写出的标准记号、长度/角度标注，以及点线圆等空间位置。
3. **逻辑推导负责补全**：模糊或缺失条件先由题干、已知条件、所求目标和几何规则推导，不用大视觉模型猜测。
4. **可追溯性**：每个条件必须标记来源：
   - `observed`：OCR 或图中符号直接读取；
   - `given`：题目文字明确给定；
   - `inferred`：由规则或推理推导得到。
5. **不静默上传**：本地模式失败不得自动把图片或题目发至 N.E.K.O API。
6. **不从零训练视觉大模型**：如需训练，仅训练小型符号检测或图文对齐模块。

## 4. 目标架构

```text
题目图片 / 文字
  ├─ 本地文字与公式 OCR
  ├─ 轻量图元/符号检测（按需）
  ├─ 题干条件解析
  └─ 图文条件融合
          ↓
  结构化题目：实体、已知条件、图中标记、置信度、条件来源
          ↓
  几何规则 / 约束校验 + 本地逻辑讲题模型
          ↓
  可验证的解题过程与答案
```

本地模式关闭时，继续使用原有 N.E.K.O API 的图像/文字模型路径。

## 5. 本地模型与运行策略

### 基础组件

- 公式识别：优先实测 RapidLaTeXOCR 与 PP-FormulaNet-S。
- 文字讲题：0.5B–1B、4-bit 的本地量化文字模型。
- 推理引擎：轻量本地运行器；模型可通过本机回环地址提供统一接口。
- 几何推理：规则、约束和验证优先；语言模型负责解释、规划和调用推理能力。

### 资源管理

```text
未加载
  → 首次使用时加载对应组件
  → 短时热状态，服务连续追问
  → 60–120 秒无任务后自动卸载
```

- 插件启动时不加载任何模型。
- OCR/图形识别与讲题模型串行运行，避免叠加显存。
- OCR 优先 CPU；讲题模型可使用 GPU 加速，也必须支持 CPU 降级。
- 模型下载、更新和卸载需显示占用空间，并在更新成功后清理旧版本。

## 6. 用户设置与隐私

- `使用本地模型`：总开关，默认关闭。
- 开启后显示模型下载状态、磁盘占用、当前热缓存状态和“释放本地模型内存”按钮。
- 本地模型缺失时，提示用户下载，不隐式下载大文件。
- 本地能力无法处理题目时，明确说明原因，并询问是否使用 N.E.K.O API；用户确认前不得上传任何内容。

## 7. 开发阶段

### 阶段 1：规格与选型（2–4 天）

- 固化本地/API 切换、隐私提示、结构化条件格式和资源红线。
- 核对模型及其权重、运行库、许可证与 Windows 打包方式。
- 产出模型清单和基础包体积估算。

**进入下一阶段条件：** 理论包体可控制在 1.5 GB 内，且许可与离线部署可接受。

### 阶段 2：离线基础 POC（1–2 周）

- 以独立本地运行器验证：公式 OCR → 条件抽取 → 小型本地讲题模型 → 规则校验。
- 使用至少 200–300 道真实中文题图，覆盖代数、函数、平面几何、模糊拍照和多行公式。
- 测量模型包体、冷启动、热缓存、内存/显存峰值、CPU 模式与断网行为。

**进入下一阶段条件：** 本地模式无网络调用；体积、资源和真实题图效果达到预先定义的验收线。

### 阶段 3：插件集成与资源管理（2–3 周）

- 接入本地/API 显式切换。
- 接入按需下载、完整性校验、冷启动/热缓存/自动卸载和单任务队列。
- 保证本地与 API 模式都输出统一的结构化题目与讲解结果。

### 阶段 4：图文条件融合与逻辑校验（2–4 周）

- 将题干明确条件直接写入条件库。
- 让图像只补充标准符号和空间定位。
- 校验条件冲突、可推导缺失条件，以及不影响答案的模糊信息。
- 仅当不同解释会导致不同答案时，才提示用户补充信息。

### 阶段 5：可选平面几何增强包（3–6 周）

- 支持点、线段、圆、平行、垂直、等长、等角、角度和长度标注。
- 使用小型检测器、OpenCV 和规则引擎；不引入通用视觉大模型。
- 作为按需下载的增强包，不增加基础版用户的磁盘负担。

### 阶段 6：发布验收（1–2 周）

- 在 8GB 无独显、16GB 普通设备和开发高配设备上测试。
- 验收断网、本地隐私、模型更新清理、空闲释放、CPU 降级和 API 回退授权。

## 8. 风险与处理

| 风险 | 处理方式 |
| --- | --- |
| 小型语言模型编造条件 | 条件来源标记；规则校验；禁止把推导项当作题目已知。 |
| OCR 误读数字、下标、负号 | 保存置信度；局部复核；不允许讲题模型无依据补写。 |
| 图中唯一条件无法读取 | 先尝试逻辑推导；只有答案会因此分叉时才提示用户。 |
| 复杂证明、立体图或手绘图能力不足 | 首版限定常见中学平面几何和公式题；逐步扩展。 |
| 模型/依赖导致包体膨胀 | POC 以实际 Windows 打包体积为准；未达标即更换方案。 |
| 本地失败时的隐私风险 | 禁止静默 API 回退，必须经过用户确认。 |

## 9. 总投入估算

- 单人顺序推进：约 8–14 周。
- 前两个阶段：约两周即可判定基础本地方案是否值得继续投入。
- POC 未达到 1.5 GB、性能或正确性门槛时，不进入几何增强开发。

## 10. 阶段 1 详细执行计划：规格与选型

### 10.1 阶段目标

在不改动现有讲题流程的前提下，完成基础本地版的可行性设计与候选组件清单。阶段一不训练模型、不下载大规模数据集、不开发几何视觉模型；其唯一目标是为阶段二 POC 设定可验证的输入、输出、资源和隐私边界。

### 10.2 范围

**本阶段要完成：**

- 定义本地模式与 N.E.K.O API 模式的用户行为和回退规则。
- 定义本地识别、条件融合和讲题模块之间的结构化数据契约。
- 选择进入 POC 的公式 OCR 候选、文字逻辑模型候选和本地推理运行方式。
- 核对模型权重、运行库、许可证、Windows 离线运行与实际安装包体积风险。
- 定义 POC 题集、指标、基准设备和 Go / No-Go 决策门。

**本阶段不做：**

- 不把任何模型正式打进插件或默认下载安装到用户设备。
- 不替换现有 N.E.K.O API 路径。
- 不训练通用视觉模型或大语言模型。
- 不承诺手写题、立体几何、复杂竞赛证明题的本地支持。

### 10.3 四天执行安排

#### 第 1 天：产品行为和数据契约

1. 固化三种运行状态：
   - `api`：保持当前 N.E.K.O API 行为；
   - `local`：仅调用本地组件；
   - `local_failed`：本地失败后等待用户明确授权，不能自动上传。
2. 明确本地模式开关默认关闭；模型下载、删除和更新均须由用户发起或确认。
3. 定义结构化题目最小契约，例如：

```json
{
  "question_text": "...",
  "entities": ["A", "B", "C"],
  "conditions": [
    {
      "expression": "parallel(AB, CD)",
      "source": "given",
      "confidence": 1.0
    }
  ],
  "formulas": [{"latex": "...", "confidence": 0.95}],
  "image_observations": [],
  "unresolved": []
}
```

4. 规定推理输出必须引用条件 ID，且将 `inferred` 条件与 `given` 条件分开显示和存储。

**当天产出：** 本地模式交互说明、回退隐私说明、结构化题目 JSON 草案。

#### 第 2 天：候选组件与发布可行性审查

1. 公式 OCR 至少保留两个候选进入 POC：
   - RapidLaTeXOCR：优先验证部署便利性与体积；
   - PP-FormulaNet-S：优先验证中文公式与真实试卷效果。
2. 选择 2 个本地文字逻辑模型候选：参数规模限定在 0.5B–1B，优先选择可稳定 4-bit 量化、可本地 CPU/GPU 运行、许可证可用于目标发布方式的模型。
3. 确定本地运行器原则：
   - 独立于插件 Python 依赖，避免把深度学习框架整体塞入插件运行环境；
   - 仅监听 `127.0.0.1`；
   - 支持模型按需加载、卸载、状态查询与进程异常重启；
   - 只提供最小本地接口，不暴露网络服务。
4. 为每个候选填写清单：模型文件、量化格式、运行库大小、许可证、Windows 可用性、CPU/GPU 支持、下载来源、哈希校验方式、已知限制。

**当天产出：** 候选组件对比表、许可证与运行库风险表、理论基础包体积拆分表。

#### 第 3 天：POC 评测设计与资源预算

1. 建立 200–300 题的私有验证题集；图片不默认进入产品数据或训练数据。
2. 题集最低覆盖：
   - 印刷公式、分式/根式/上下标、多行公式；
   - 中文题干、选择题、填空题和计算题；
   - 题干明确条件的平面几何题；
   - 常见拍照问题：倾斜、低光、轻度模糊、局部遮挡。
3. 为每题手工记录：题干文本、公式 LaTeX、明确已知条件、答案，以及是否需要图中独有信息。
4. 固化资源测试矩阵：
   - 8GB RAM、无独显的 CPU 基线；
   - 16GB RAM 的普通用户设备；
   - 16GB 显存开发设备。
5. 固化测量项：安装包、模型下载量、冷启动、连续三题热状态、120 秒空闲释放、峰值 RAM、峰值显存、峰值 CPU、断网行为与网络请求。

**当天产出：** POC 数据说明、标注格式、设备矩阵、自动/手动测量表。

#### 第 4 天：验收门与阶段二实施说明

1. 确定阶段二的最小成功标准：
   - 基础包理论与实测均不超过 1.5 GB；
   - 本地空闲显存为 0；
   - 单任务峰值内存和显存均不超过 3 GB；
   - 本地模式断网可工作，且抓包未发现 N.E.K.O API 请求；
   - 题干明确条件的提取优先保证高精确率，不能以“模型猜测”补齐；
   - 对不能确定的内容能输出未解决项，而非伪造条件。
2. 设定量化评测指标：
   - 公式：渲染等价率、字符/Token 错误率、平均耗时；
   - 题干：文本错误率、明确条件精确率/召回率；
   - 讲题：答案正确性、条件引用完整率、无依据条件率；
   - 性能：冷启动、热状态、空闲释放和设备兼容性。
3. 输出 Go / No-Go 决策：
   - **Go**：至少一套候选同时满足许可、离线、包体、资源和基本正确性门槛；
   - **调整后重测**：仅某一类候选不达标，存在明确替代项；
   - **No-Go**：在 1.5 GB 与资源红线内不存在可用组合，则保留 API 模式，不进入正式集成。

**当天产出：** 阶段二 POC 任务书、验收表与决策记录模板。

### 10.4 阶段一完成定义

阶段一完成时，项目必须拥有以下可审查材料：

- 本地/API 用户流程与隐私文案；
- 结构化题目与条件来源的数据契约；
- 至少两种 OCR 候选和两种文字模型候选的对比清单；
- 许可证、Windows 运行、包体和资源风险记录；
- 200–300 题 POC 题集规范与标注方案；
- 阶段二的量化验收表和明确的 Go / No-Go 门槛。

在这些材料未齐全前，不进入模型集成或几何增强开发。
