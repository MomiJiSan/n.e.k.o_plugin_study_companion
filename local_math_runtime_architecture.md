# 轻量本地数学模型运行架构与实施计划

## 1. 首版目标

首版不以超过云端数学 OCR、通用视觉模型或在线解题产品为目标。目标只包括：

1. 用户可明确选择使用本地模型或 N.E.K.O API；
2. 本地模型能在 Windows 上离线下载、启动、推理和卸载；
3. 未启用本地模型时不加载模型、不占显存；
4. 本地模型按需冷启动、短时热缓存、空闲后自动释放资源；
5. 基础数学题（文字题、公式题、普通图片题）可以获得可用结果；
6. 本地失败时不静默上传内容，是否使用 API 始终由用户决定。

首版不包含：通用几何视觉理解、复杂图文条件融合、模型训练、通用大视觉模型、与云端模型准确率竞争。

## 2. 架构总览

```text
┌───────────────────────────────────────────────────────────┐
│ N.E.K.O 学习插件                                            │
│                                                           │
│  设置页面 / 学习 UI                                         │
│         │                                                 │
│  InferenceModeRouter                                       │
│    ├── APIAdapter ─────────────→ 现有 N.E.K.O API 路径       │
│    └── LocalRuntimeClient ────→ 127.0.0.1 本地运行器        │
│         │                                                 │
│  LocalRuntimeSupervisor                                    │
│    ├── 启动、健康检查、异常重启、停止                        │
│    └── 仅在本地模式且需要服务时启动                          │
└───────────────────────────────────────────────────────────┘
                                   │
                         回环地址 + 随机会话令牌
                                   │
┌───────────────────────────────────────────────────────────┐
│ Local Math Runtime（独立子进程）                            │
│                                                           │
│  JobQueue（单任务串行）                                    │
│    ├── ModelRegistry / ModelManager                         │
│    │     ├── manifest、下载、SHA-256 校验、更新、删除       │
│    │     └── 模型版本与许可信息                             │
│    ├── LifecycleController                                  │
│    │     ├── unloaded → loading → ready → busy → warm      │
│    │     └── idle timeout → unloading → unloaded           │
│    ├── FormulaRecognizerAdapter（可选）                     │
│    ├── LocalReasonerAdapter                                 │
│    └── ResourceMonitor（RAM / VRAM / CPU）                  │
└───────────────────────────────────────────────────────────┘
```

### 2.1 为什么使用独立本地运行器

- 不将 ONNX、量化推理或未来模型的依赖直接混入插件主进程。
- 模型崩溃、显存不足或更新失败不会拖垮插件本身。
- 本地模型可统一按需启动、单任务排队和释放资源。
- 后续替换 OCR 或文字模型时，不需要重写插件 UI 与 API 模式。

### 2.2 进程与网络边界

- 本地运行器由插件父进程启动和终止。
- 仅绑定 `127.0.0.1` 的随机端口；不得监听局域网或公网地址。
- 每次启动生成随机会话令牌，插件请求必须携带该令牌。
- 禁止运行器自行访问 N.E.K.O API；本地模式的网络请求默认应为零。
- 模型下载只由用户发起或确认；下载来源、大小和版本必须在 UI 显示。

## 3. 用户模式与路由

### 3.1 配置

```text
inference_mode = api | local
local_profile = lite
cache_policy = memory_saving | balanced | continuous
allow_api_fallback_after_confirmation = true
```

默认值：

```text
inference_mode = api
cache_policy = balanced
allow_api_fallback_after_confirmation = true
```

### 3.2 请求路由

```text
用户提交文字/图片题
  ├─ api 模式
  │    └─ 直接沿用现有 N.E.K.O API 调用
  │
  └─ local 模式
       ├─ 确认本地运行器与所需模型可用
       ├─ 调用本地运行器
       ├─ 成功：显示本地结果
       └─ 失败：显示本地失败原因
              └─ 用户明确选择“改用 N.E.K.O API”后才发送内容
```

不允许的行为：本地模式失败后自动切到 API；模型缺失时静默下载；关闭本地模式后继续保留模型在显存中。

## 4. 模型配置与首版能力

### 4.1 `lite` 本地模型档案

首版使用可替换的两段式能力，而不是常驻通用视觉大模型：

```text
图片题
  → 公式/文字识别组件（按需）
  → 卸载或转入短时缓存
  → 小型本地文字讲题模型
  → 结果
```

- 公式识别候选：RapidLaTeXOCR、PP-FormulaNet-S；最终以 POC 结果选择其一。
- 讲题模型候选：0.5B–1B、4-bit 量化、Windows 可运行的文字模型；最终以许可证、包体、速度和基础题效果选择其一。
- 不要求视觉组件独立理解复杂几何；识别失败时允许用户使用 API 模式。

### 4.2 模型包布局

```text
<user-model-directory>/
  manifest.json
  formula-recognizer/<version>/...
  local-reasoner/<version>/...
  downloads/*.partial
```

`manifest.json` 至少记录模型 ID、版本、文件大小、SHA-256、下载来源、许可证、所需运行器版本、CPU/GPU 能力、安装状态和最后使用时间。

下载流程：下载到 `.partial` → SHA-256 校验 → 原子移动至版本目录 → 更新 manifest。失败时删除临时文件，不得加载不完整模型。

## 5. 本地运行器接口

接口只服务于插件，不作为通用本机公开 API。

| 接口 | 用途 |
| --- | --- |
| `GET /health` | 进程与版本健康检查 |
| `GET /runtime/status` | 已加载模型、任务状态、RAM/VRAM、热缓存倒计时 |
| `POST /models/install` | 下载指定模型；返回进度和校验结果 |
| `POST /models/uninstall` | 删除用户确认的模型版本 |
| `POST /runtime/unload` | 立即卸载一个或全部模型 |
| `POST /v1/math/recognize` | 图片/文字转为题目文本、公式与置信度 |
| `POST /v1/math/explain` | 接收已识别文本，调用本地讲题模型 |

首版统一响应最低字段：

```json
{
  "request_id": "...",
  "mode": "local",
  "text": "...",
  "formulas": [{"latex": "...", "confidence": 0.95}],
  "answer": "...",
  "warnings": [],
  "runtime": {
    "loaded_models": ["..."],
    "used_gpu": false
  }
}
```

复杂的条件溯源、几何实体图和规则证明为后续扩展字段，不阻塞首版。

## 6. 冷启动、热缓存与资源策略

### 6.1 状态机

```text
unloaded
  → loading
  → ready
  → busy
  → warm_idle
  → unloading
  → unloaded
```

### 6.2 规则

- 插件启动：不启动运行器，不加载模型。
- 用户首次在本地模式提交题目：启动运行器并按需加载所需组件。
- 同一任务中的识别与讲题模型不同时占用 GPU；采用单任务队列。
- `memory_saving`：任务完成后或 30 秒空闲后卸载。
- `balanced`：90 秒空闲后卸载，作为默认值。
- `continuous`：10 分钟空闲后卸载，仅由用户主动选择。
- 用户点击“释放本地模型内存”：取消排队任务后立即卸载所有模型。
- 显存不足、内存不足或进程崩溃：返回可解释错误；不自动回退 API。

### 6.3 首版性能预算

| 项目 | 目标 |
| --- | ---: |
| 基础模型与专用运行组件磁盘占用 | ≤ 1.5 GB |
| 未使用本地能力的额外显存 | 0 |
| 未使用本地能力的额外内存 | ≤ 300 MB |
| 单任务峰值 RAM | ≤ 3 GB |
| 单任务峰值 VRAM | ≤ 3 GB |
| 默认热缓存窗口 | 90 秒 |

这些是验收目标，必须以 Windows 真机实测结果为准。

## 7. 分阶段详细实施计划

### 阶段 A：本地运行器骨架与模式开关（4–6 个工作日）

**实现内容**

1. 新增本地模式配置与设置页开关，默认仍为 API 模式。
2. 实现 `InferenceModeRouter`：API 模式走现有网关；本地模式走 `LocalRuntimeClient`。
3. 实现 `LocalRuntimeSupervisor`：按需创建、健康检查、停止和异常重启本地运行器进程。
4. 实现回环地址、随机端口和会话令牌传递；禁止外部监听。
5. 实现最小运行器：`/health`、`/runtime/status`、`/runtime/unload`，暂不加载真实模型。
6. UI 显示本地运行器状态；本地失败时显示“使用 N.E.K.O API”按钮，但不得自动调用。

**验收**

- 关闭本地模式时，现有 API 流程没有行为变化。
- 开启本地模式后，运行器仅在首次请求时启动。
- 运行器退出或异常时，插件不崩溃。
- 运行器未监听非回环地址，且 API 回退必须点击确认。

### 阶段 B：模型包与下载管理（4–6 个工作日）

**实现内容**

1. 定义并实现 `manifest.json` 数据格式与版本状态。
2. 实现模型目录选择、剩余磁盘空间检查、下载进度、暂停/失败提示。
3. 实现 `.partial` 下载、SHA-256 校验、原子安装、版本清理和用户确认的卸载。
4. UI 显示每个模型的名称、版本、文件大小、许可证、安装状态和删除按钮。
5. 模型文件不进入插件源码包，不在首次启用开关时隐式下载。

**验收**

- 断网、文件损坏、磁盘不足和重复安装均有明确错误与可恢复状态。
- 未通过哈希校验的文件绝不能被加载。
- 更新后只保留当前版本；旧版本和临时文件能够正确清理。

### 阶段 C：首版本地推理链路 POC（7–10 个工作日）

**实现内容**

1. 为两个公式 OCR 候选实现可替换适配器。
2. 为两个 0.5B–1B、4-bit 文字模型候选实现可替换适配器。
3. 实现 `/v1/math/recognize` 与 `/v1/math/explain`，先保证输入输出一致和错误可解释。
4. 图片任务按“识别 → 讲题”串行执行；文字任务直接进入本地讲题模型。
5. 记录仅限本机的性能事件：模型加载时间、推理时间、RAM、VRAM、失败原因；默认不上传。

**验收**

- 至少一个 OCR + 一个文字模型组合能在断网条件下完成基础题处理。
- 基础模型总占用、单任务 RAM/VRAM 达到第 6.3 节预算，或明确记录偏差。
- 本地模式下网络抓包不出现 N.E.K.O API 请求。
- 任何一个模型失败时，UI 能区分“模型缺失、资源不足、识别失败、推理失败”。

### 阶段 D：资源管理、冷启动和热缓存（4–6 个工作日）

**实现内容**

1. 实现模型状态机和单任务队列。
2. 实现三档缓存策略、90 秒默认空闲计时、手动释放内存。
3. 实现识别与讲题模型的串行加载与卸载；禁止同时常驻 GPU。
4. 实现内存/显存阈值检查、CPU 降级、队列取消和进程崩溃后的资源回收。
5. 增加本地状态面板：已加载模型、当前任务、RAM/VRAM、预计释放时间。

**验收**

- 插件空闲、运行器空闲、模型热缓存、缓存超时后的资源状态均可观测。
- 90 秒空闲后显存归零；手动释放立即生效。
- 连续三题在热状态下不重复加载模型；不同模型不发生显存叠加。

### 阶段 E：插件体验与安全收口（5–7 个工作日）

**实现内容**

1. 完成设置页：模式开关、模型安装/删除、缓存策略、模型目录、状态与手动释放。
2. 完成题目页面状态：本地加载中、识别中、讲解中、资源不足、模型缺失和本地失败。
3. 完成 API 回退确认框，明确说明将发送哪些内容。
4. 完成离线使用说明、存储占用说明和隐私说明。
5. 将本地与 API 结果统一为现有讲题 UI 可展示的格式。

**验收**

- 用户无需理解模型术语，也能完成“开启 → 下载 → 本地做题 → 释放资源 → 切回 API”。
- 关闭本地模式后不遗留运行器、模型进程或显存。
- API 回退需要一次明确、可取消的用户操作。

### 阶段 F：性能与发布验收（5–7 个工作日）

**测试设备**

- 8GB RAM、无独显：验证 CPU 降级与资源安全。
- 16GB RAM、普通独显：验证默认体验。
- 16GB 显存开发设备：验证 GPU 加速、峰值和热缓存。

**测试项目**

- 安装与更新后的真实磁盘占用；
- 冷启动：首次本地文字题、首次图片题；
- 热状态：连续三题与连续追问；
- 30/90/600 秒空闲后的 RAM/VRAM；
- 断网、下载中断、损坏模型、资源不足、运行器崩溃；
- 本地模式网络抓包与 API 回退授权。

**发布门槛**

- 基础包 ≤1.5 GB；
- 空闲不占显存；
- 本地模式不静默联网；
- API 模式无回归；
- 异常可恢复且不影响插件稳定性；
- 已明确记录实际性能边界与首版不支持的题型。

## 8. 后续扩展（不阻塞首版）

首版稳定后再评估：

- 更强的公式/手写识别；
- 常见平面几何图元和标准符号检测；
- 图文条件融合、条件来源展示和规则校验；
- 可选几何增强模型包；
- 基于用户明确授权数据的小模型微调。

所有扩展必须继续遵守：可选下载、按需加载、单模型资源预算、用户可控 API 回退和本地隐私优先。
